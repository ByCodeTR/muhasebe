# Production environment variables template
# Copy this file and fill in the values for deployment

# =============================================================================
# BACKEND (Render)
# =============================================================================

# Database - auto-provided by Render when linking database
DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<database>

# Security
SECRET_KEY=<generate-a-secure-random-string>
DEBUG=false
ALLOWED_ORIGINS=https://your-frontend.vercel.app

# Telegram Bot
TELEGRAM_BOT_TOKEN=<your-bot-token-from-botfather>
TELEGRAM_WEBHOOK_SECRET=<generate-a-random-string>

# OCR (pre-installed on Render via Dockerfile)
TESSERACT_PATH=/usr/bin/tesseract

# Optional: Cloud storage
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
# AWS_S3_BUCKET=<bucket-name>
# AWS_ACCESS_KEY_ID=<key>
# AWS_SECRET_ACCESS_KEY=<secret>

# =============================================================================
# FRONTEND (Vercel)
# =============================================================================

# API URL - set in Vercel environment variables
NEXT_PUBLIC_API_URL=https://your-api.onrender.com

# =============================================================================
# RENDER SETUP STEPS
# =============================================================================
# 1. Create a new Web Service from GitHub repo
# 2. Select "Docker" as runtime
# 3. Set root directory to "backend"
# 4. Create PostgreSQL database and link it
# 5. Add environment variables above
# 6. Deploy!
#
# After deployment, set Telegram webhook:
# GET https://your-api.onrender.com/telegram/set-webhook?webhook_url=https://your-api.onrender.com/telegram/webhook

# =============================================================================
# VERCEL SETUP STEPS
# =============================================================================
# 1. Import GitHub repo to Vercel
# 2. Set root directory to "frontend"
# 3. Add NEXT_PUBLIC_API_URL environment variable
# 4. Deploy!
